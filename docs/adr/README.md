# Architecture Decision Records

Short records of significant technical decisions, why they were made, and
what to revisit them for. Add a new numbered file (`000N-title.md`) rather
than editing history — if a decision changes, write a new ADR that
supersedes the old one and say so in both files.
